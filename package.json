{
  "name": "grannybackend",
  "version": "1.0.0",
  "description": "back end for grannycb",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json -p 3001"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
